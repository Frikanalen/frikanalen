import styled from "@emotion/styled";

export const PrimaryPopover = styled.div`
  background: ${(props) => props.theme.color.card};
  box-shadow: 2px 2px 11px 2px rgba(0, 0, 0, 0.1);

  border-radius: 4px;
`;
