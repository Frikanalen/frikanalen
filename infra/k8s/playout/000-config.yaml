apiVersion: v1
kind: ConfigMap
metadata:
  name: playout-config
  namespace: default
data:
  config.yaml: |
    mediaAssets:
      scheme: file
    graphqlEndpoint: https://frikanalen.no/graphql
    playerClass: vision.players.casparcg_player:CasparCGPlayer
    casparHost: 192.168.3.35
    useJukebox: true
    logFormat: json
